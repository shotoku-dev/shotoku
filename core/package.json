{
  "name": "@shotoku/core",
  "version": "0.1.0",
  "publishConfig": { "access": "public" },
  "type": "module",
  "description": "Core daemon, ledger, and policy engine for Shotoku.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
